{
    IBClasses = (
        {CLASS = ASHistoryView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            ACTIONS = {
                changeBorderColour = id; 
                changeFillColour = id; 
                changeHistory = id; 
                changeLogin = id; 
                changeSave = id; 
                changeStartup = id; 
                changeUpdate = id; 
                ruleAdd = id; 
                ruleRemove = id; 
                showMain = id; 
                showMeter = id; 
                update = id; 
                viewList = id; 
            }; 
            CLASS = ASInetUsageController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                oAboutIcon = NSImageView; 
                oAddRule = NSButton; 
                oAllowancePerDay = NSTextField; 
                oAllowancePerDayLevel = NSLevelIndicator; 
                oAmountUsed = NSTextField; 
                oAverageForPeriod = NSTextField; 
                oAverageRemaining = NSTextField; 
                oAverageRemainingLevel = NSLevelIndicator; 
                oBorderColour = NSColorWell; 
                oCurrentAverage = NSTextField; 
                oCurrentAverageLevel = NSLevelIndicator; 
                oCurrentDate = NSTextField; 
                oDaysRemaining = NSTextField; 
                oDaysRemainingLevel = NSLevelIndicator; 
                oDaysRemainingPC = NSTextField; 
                oDifference = NSTextField; 
                oDifferenceLevel = NSLevelIndicator; 
                oDifferencePC = NSTextField; 
                oDownloadSpeed = NSTextField; 
                oExcessBilledAt = NSTextField; 
                oFillColour = NSColorWell; 
                oFirstDate = NSTextField; 
                oHistoryGraph = ASHistoryView; 
                oHistoryMenu = NSPopUpButton; 
                oLastDate = NSTextField; 
                oLastUpdate = NSTextField; 
                oLoginID = NSTextField; 
                oLoginPasswd = NSTextField; 
                oMainWindow = NSWindow; 
                oMeterWindow = NSWindow; 
                oPeriodStartDate = NSTextField; 
                oPrepaidMB = NSTextField; 
                oRemoveRule = NSButton; 
                oRulesList = NSTableView; 
                oSaveLogin = NSButton; 
                oShowMeter = NSButton; 
                oShowScale = NSTextField; 
                oShowStatus = NSTextField; 
                oShowVersion = NSTextField; 
                oTabView = NSTabView; 
                oTodaysUsage = NSTextField; 
                oTodaysUsageLevel = NSLevelIndicator; 
                oTopScale = NSTextField; 
                oTotalForPeriod = NSTextField; 
                oUpdateButton = NSButton; 
                oUpdateOption = NSPopUpButton; 
                oUsageRemaining = NSTextField; 
                oUsageRemainingLevel = NSLevelIndicator; 
                oUsageRemainingPC = NSTextField; 
                oViewAsTable = NSButton; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = NSLevelIndicator; LANGUAGE = ObjC; SUPERCLASS = NSControl; }
    ); 
    IBVersion = 1; 
}